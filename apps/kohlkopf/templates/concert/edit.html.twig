{% extends '@MyFrameworkCore/base_home.html.twig' %}

{% block title %}Konzert bearbeiten{% endblock %}

{% block page_title %}{{ concert.title }} bearbeiten{% endblock %}

{% block content %}
    <div class="container py-4">
        <div class="card">
            <div class="card-body">
                {% include 'concert/_form.html.twig' with { form: form, concert: concert } %}
            </div>
        </div>
    </div>
{% endblock %}

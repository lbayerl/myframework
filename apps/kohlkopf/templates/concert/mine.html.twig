{% extends 'base.html.twig' %}

{% block title %}Meine Konzerte{% endblock %}

{% block page_title %}Konzerte{% endblock %}

{% block content %}
    <div class="container py-4">
        <div class="d-flex justify-content-between align-items-center mb-3">
            <h2>Konzerte</h2>
            <a href="{{ path('concert_new') }}" class="btn btn-primary">
                <i class="bi bi-plus-lg"></i> Neues Konzert
            </a>
        </div>

        {% include 'concert/_tabs.html.twig' with { activeView: activeView } %}

        {% include 'concert/_warnings.html.twig' with { warnings: warnings } only %}

        <div class="alert alert-light border mb-3">
            <i class="bi bi-info-circle me-2"></i>
            Konzerte, bei denen du dabei bist, interessiert bist oder ein Ticket hast.
        </div>

        {% if concertData is not empty %}
            <div class="list-group">
                {% for item in concertData %}
                    {% include 'concert/_concert_item.html.twig' with { 
                        concert: item.concert,
                        userAttendance: item.userAttendance,
                        hasTicket: item.hasTicket,
                        attendingCount: item.attendingCount,
                        interestedCount: item.interestedCount,
                        showStatus: true,
                        concertWarnings: warnings.concertWarnings[item.concert.id] ?? []
                    } only %}
                {% endfor %}
            </div>
        {% else %}
            <div class="alert alert-info">
                <i class="bi bi-calendar-x me-2"></i>
                Du hast noch keine Konzerte als „Dabei" oder „Interessiert" markiert und keine Tickets.
                <br>
                <a href="{{ path('concert_index') }}" class="alert-link">Schau dir die kommenden Konzerte an!</a>
            </div>
        {% endif %}
    </div>
{% endblock %}
